import React, { useEffect } from 'react';import './analysis.scss';import analysisService from '../../services/firstService';import DataTable from '../../components/DataTable/DataGrid';import { analysisTableColumns } from '../../helpers/DataTableColumns/analysisTable';import { AnalysisList } from '../../_mock/analysisList';export const Analysis: React.FC<{}> = props => {    useEffect(() => {        getAnalysisList();    }, []);    const getAnalysisList = () => {        analysisService            .getAnalysisList()            .then(res => console.log(res.data))            .catch(e => console.log(e));    };    return (        <>            <DataTable                columns={analysisTableColumns}                rows={AnalysisList.content}                // onRowClick={() => alert('clicked')}            />        </>    );};