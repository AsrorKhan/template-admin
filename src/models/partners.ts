interface IPartnersList {    id: number | null;    login: string;    firstName: string;    lastName: string | null;    percent: string;    email: string | null;    imageUrl: string | null;    inpayClientId: number | null;    activated: boolean;    langKey: string;    createdBy: string;    createdDate: string;    lastModifiedBy: string;    lastModifiedDate: string;    authorities: string[];}export interface IPartners {    content: IPartnersList[];    totalElement: number | null;    currentPage?: 0;}