import { createSlice, PayloadAction } from '@reduxjs/toolkit';import { IAnalysis } from '../../models/analysis';const initialState: IAnalysis = {    content: [],    totalElement: 0,    currentPage: 0,};const ExampleStore1 = createSlice({    name: 'analysis',    initialState,    reducers: {        setAnalysis(state: IAnalysis, action: PayloadAction<IAnalysis>) {            state.content = action.payload.content;            state.currentPage = action.payload.currentPage;            state.totalElement = action.payload.totalElement;        },    },});export const { setAnalysis } = ExampleStore1.actions;export default ExampleStore1.reducer;