import { createSlice, PayloadAction } from '@reduxjs/toolkit';import { IHome } from '../../models/home';const initialState: IHome = {    content: [],    totalElement: 0,    currentPage: 0,};const ExampleStore2 = createSlice({    name: 'home',    initialState,    reducers: {        setAnalysis(state: IHome, action: PayloadAction<IHome>) {            state.content = action.payload.content;            state.currentPage = action.payload.currentPage;            state.totalElement = action.payload.totalElement;        },    },});export const { setAnalysis } = ExampleStore2.actions;export default ExampleStore2.reducer;