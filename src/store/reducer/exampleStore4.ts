import { createSlice, PayloadAction } from '@reduxjs/toolkit';import { ITotalPrice } from '../../models/totalPrice';const initialState: ITotalPrice = {    installmentAmount: 0,    supplierCostAmount: 0,};const ExampleStore4 = createSlice({    name: 'totalPrice',    initialState,    reducers: {        setTotalPrice(state: ITotalPrice, action: PayloadAction<ITotalPrice>) {            state.supplierCostAmount = action.payload.supplierCostAmount;            state.installmentAmount = action.payload.installmentAmount;        },    },});export const {} = ExampleStore4.actions;export default ExampleStore4.reducer;